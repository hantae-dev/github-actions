name: issue-workflow
on:
  issues:
    types: [opened]

# 트리거 됬을떄 어떤 잡을 실행할지
jobs:
  issue-job:
    runs-on: ubuntu-latest # 어떤 머신 위에서 실행할지
    steps: # 실행시킬 잡 순서
      - name: step1
        run: echo hello 이슈 발견
      - name: step2
        run: echo 처리하셈
