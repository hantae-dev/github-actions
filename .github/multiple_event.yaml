name: multple-event-workflow
on:
  push:
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  multple-event-job:
  runs-on: ubuntu-latest # 어떤 머신 위에서 실행할지
  steps: # 실행시킬 잡 순서
    - name: step1
      run: echo hello 이슈 발견
    - name: step2
      run: echo 처리하셈
